node_modules/
build/
dist/
coverage/
.DS_Store
*.log
.env*
!.env.example
.firebase/

# Assets de fond du feed — lourds, hors git (servis via Firebase Hosting).
# 4K sources (transcodage) + sortie optimisée 1080p.
background_bank/
bank_png/
public/background_bank/
_background_bank_4k_backup/

# Worker local de génération audio
worker/serviceAccount.json    # clé de compte de service Firebase — JAMAIS committer
worker/*.json                 # autres clés éventuelles (sauf .env.example déjà géré)
!worker/package*.json
__pycache__/
*.pyc
.venv/
venv/
